<nav class="navbar navbar-expand-md" style="background: #8582bb;">
  <div class="container-fluid">
    <a class="navbar-brand me-lg-5" href="/">
      <img
        src="https://cdn-icons-png.flaticon.com/128/6823/6823086.png"
        alt="Logo"
        width="35"
        height="35"
        class="d-inline-block align-text-top"
      />
      <span class="text-center fw-semibold">Employee Review System</span>
    </a>

    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <% if (locals.user) { %>
        <ul class="navbar-nav me-auto mb-2 mb-lg-0 ms-5">
          <li class="nav-item ms-lg-4" style="margin-left: -50px;">
            <a class="nav-link fw-semibold" aria-current="page" href="/">
              <i class="fas fa-home"></i> Home
            </a>
          </li>
          <% if (locals.user.role === 'admin') { %>
            <li class="nav-item ms-lg-4" style="margin-left: -150px;">
              <a class="nav-link fw-semibold" aria-current="page" href="/add-employee">
                <i class="fas fa-plus"></i> Add employee
              </a>
            </li>
          <% } %>
        </ul>

        <form class="d-flex justify-content-end">
          <div class="btn" style="margin-right: 10px; font-weight: bold;">
            </i> <%= locals.user.username %>
          </div>
          <a class="btn" style="font-weight: bold;" href="/sign-out">
           Sign Out
          </a>
        </form>
      <% } %>
    </div>
  </div>
</nav>
